// Public, domain-restricted MapKit JS token only. Never put a .p8 private key here.
// Setup: docs/apple-map-setup.md
export const appleMapConfig = {
  token: '',
  // Current incident's confirmed coordinate + bearing, with metre-based polygons.
  // Do not substitute the teammate's Uiwang sample for this Hwagok incident.
  scene: null,
};
