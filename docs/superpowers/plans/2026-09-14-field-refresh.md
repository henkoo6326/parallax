# PARALLAX white-first field refresh

The user explicitly authorized implementing and adding appropriate interactions without a new approval gate.

1. Default to white, replace yellow/olive surfaces with cool gray, charcoal and emerald; retain dark as an option.
2. Replace the default aerial image map with a labelled, interactive vector street map. Keep generated white/dark aerial assets in a separate drone view. Zoom/pins share coordinates; floor, selected zone and layer visibility persist.
3. Remove bodycam from UI, source data and old saved camera selection. Create an independent first-person AR scene, HUD toggle and capture/bookmark interaction.
4. Insert the user's exact glasses image into the device page and AR source device card. Add clickable device feature hotspots.
5. Add local feedback: pressed states, panel entry, source change, capture flash, linked capture thumbnails, map layers, real simulation playback, motion reduction support.
6. Verify migration, map/drone switching, layer selection, media exclusion, playback bounds, persistence and two fold sizes. Update design notes and leave white default map preview open.

Assets are illustrative; no real media, hardware or external recipients are connected.
